import React from 'react'
import { Spinner } from 'native-base'

const CustomSpinner = () => {
  return (
    <Spinner size={70} color='blue.600' shadow={'4'} />
  )
}

export default CustomSpinner